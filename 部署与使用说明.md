# RecoveryTool 部署与使用说明

## 一、发布方式

本项目使用以下方式发布：

- 发布模式：`Release`
- 运行平台：`win-x64`
- 部署方式：`Self-contained`
- 目标系统：Windows 10 / Windows 11

Self-contained 表示目标电脑无需单独安装 .NET Runtime。

## 二、复制到其他电脑

请将以下目录中的全部文件和文件夹完整复制到目标电脑：

```text
bin\Release\net8.0-windows\win-x64\publish\
```

不要只复制 `RecoveryTool.exe`，因为程序还依赖同目录下的 .NET Runtime 文件、DLL 文件和配置文件。

复制后应保持类似以下结构：

```text
publish\
├── RecoveryTool.exe
├── RecoveryTool.dll
├── *.dll
├── *.json
├── Config\
│   └── config.json
├── Scripts\
│   ├── install.ps1
│   └── reset.ps1
└── 其他运行时文件和语言资源目录
```

在目标电脑上双击运行：

```text
publish\RecoveryTool.exe
```

## 三、运行要求

- Windows 10 或 Windows 11
- x64 设备
- 需要管理员权限
- 系统安装功能需要可用的 Windows ISO 文件
- 执行重置或安装前，请备份重要数据

程序启动时会请求管理员权限，这是系统恢复和安装功能所必需的。

## 四、功能说明

### 1. 重置系统

点击“重置系统”后，程序会弹出二次确认，然后调用 Windows 官方的“重置此电脑”功能。

该操作可能会：

- 删除用户数据
- 删除已安装软件
- 重装或恢复 Windows

### 2. 安装系统

点击“安装系统”后，程序会先打开 ISO 下载引导窗口。

微软 Windows 11 官方下载地址：

<https://www.microsoft.com/zh-cn/software-download/windows11>

下载 ISO 后，返回程序并选择本地 `.iso` 文件，确认后启动 Windows 安装程序。

## 五、运行目录

程序运行过程中会使用以下目录：

```text
Config\    配置文件
ISO\       ISO 镜像存放目录
Scripts\   PowerShell 辅助脚本
Logs\      操作日志
Temp\      临时文件
```

其中 `Config` 和 `Scripts` 文件夹必须保留。`Logs`、`Temp` 和 `ISO` 文件夹可以由程序首次运行时创建。

## 六、重新发布命令

在项目根目录执行：

```powershell
dotnet publish .\RecoveryTool.csproj -c Release -r win-x64 --self-contained true
```

发布结果位于：

```text
bin\Release\net8.0-windows\win-x64\publish\
```

## 七、安全注意事项

RecoveryTool 会调用 Windows 的系统恢复和安装功能，可能造成不可逆的数据丢失。

使用前请确认：

1. 已备份重要文件。
2. 已接通电源或保证电池电量充足。
3. 选择了正确的 ISO 文件和目标电脑。
4. 执行前已阅读程序的二次确认提示。

